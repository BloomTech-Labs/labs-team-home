mutation CreateNewUser($firstName: String!, $lastName: String!, $email: String!) {
    addUser(input:  {
            firstName: $firstName
            lastName: $lastName
            email: $email
            }) {
        _id
    }
}
