query FindMessagesByTeam($teamId: ID!) {
    findMessagesByTeam(input: {
                       team: $teamId
                       }) {
        _id
        title
        content
        images
        tags {
            name
        }
        comments
        createdAt
        updatedAt
    }
}
