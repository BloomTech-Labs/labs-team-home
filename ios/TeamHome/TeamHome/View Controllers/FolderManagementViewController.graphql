mutation MoveToFolder($id: ID!, $title: String!){
    updateFolder(input: {
                  id: $id
                  title: $title
                  
                  }) {
        _id
        title
    }
}

