mutation AddNewMessage($title: String!, $team: String!, $content: String!, $images: [String], $tags: [String]) {
    addMessage(input: {
               title: $title
               team: $team
               content: $content
               images: $images
               tags: $tags
               }) {
        title
        _id
    }
}
