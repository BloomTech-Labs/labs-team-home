query FindTeamById($id: ID!) {
    findTeam(input: {
             id: $id
             }) {
        name
        users {
            user {
                _id
                firstName
                lastName
                email
                phoneNumber
                avatar
            }
            admin
        }
    }
}
